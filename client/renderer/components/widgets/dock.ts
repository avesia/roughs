import { LitElement, html, css } from "lit";
import { customElement, property } from "lit/decorators.js";

@customElement("avs-dockarea")
export class DockArea extends LitElement {
    @property()

    static styles = css`
        
    `

    render() {
        return html`
            <div>

            </div>
        `
    }
}
